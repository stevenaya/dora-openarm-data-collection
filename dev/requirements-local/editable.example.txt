# Copy this file to dev/requirements-local/editable.txt and adjust paths for your machine.
#
# These packages are installed before dora build. scripts/setup-env.sh -e
# records their package names in DORA_PARENT_DEP_OVERRIDES, so node builds
# refresh these local editable packages before installing dependencies not owned
# by the parent repo.

# -e /absolute/path/to/openarm_mujoco/v2
# -e /absolute/path/to/openarm_control
# -e /absolute/path/to/openarm_driver
# -e /absolute/path/to/openarm_can
# -e /absolute/path/to/openarm_ker
